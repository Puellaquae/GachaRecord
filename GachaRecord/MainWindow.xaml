<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="GachaRecord.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GachaRecord"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="mainWindow"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>



    <Grid>


        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <CommandBar
            Grid.Row="0"
            IsOpen="False"
            DefaultLabelPosition="Right"
            Margin="14, 0, 150, 0">
            <AppBarButton Icon="Sync" Label="更新" Click="OnUpdate">
            </AppBarButton>
            <AppBarButton Icon="OpenFile" ToolTipService.ToolTip="导入 UIGF" Label="导入" Click="OnImport">
            </AppBarButton>
            <AppBarButton Icon="Save" ToolTipService.ToolTip="导出为 UIGF" Label="导出" Click="OnExport">
            </AppBarButton>
            <AppBarButton Icon="Setting" Label="设置" >
                <AppBarButton.Flyout>
                    <MenuFlyout>
                        <ToggleMenuFlyoutItem Text="测试模式" IsChecked="False" Click="OnSwitchDebugMode"/>
                        <MenuFlyoutItem Text="删除数据" Click="OnClearData"/>
                        <MenuFlyoutSubItem Text="UID">
                            <RadioMenuFlyoutItem Text="TODO" GroupName="UIDGroup" IsChecked="True"/>
                        </MenuFlyoutSubItem>
                    </MenuFlyout>
                </AppBarButton.Flyout>
            </AppBarButton>
            <CommandBar.Content>
                <StackPanel Orientation="Horizontal">
                    <SelectorBar x:Name="GameSelector" SelectionChanged="GameSelectorChanged">
                        <SelectorBarItem x:Name="GameSelectorHk4e" Text="原神" IsSelected="True" />
                        <SelectorBarItem x:Name="GameSelectorHkrpg" Text="崩铁" />
                        <SelectorBarItem x:Name="GameSelectorNap" Text="绝区零" />
                    </SelectorBar>
                </StackPanel>
            </CommandBar.Content>
        </CommandBar>
        <TeachingTip x:Name="ProcessTip">
            <TeachingTip.Content>
                <ProgressBar x:Name="ProcessTipBar" IsIndeterminate="True" Margin="0,12,0,4"/>
            </TeachingTip.Content>
        </TeachingTip>
        <Frame Grid.Row="1" x:Name="ContentFrame"></Frame>
    </Grid>
</Window>
