<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="GachaRecord.Nap"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GachaRecord"
    xmlns:ui="using:GachaRecord.UI"
    xmlns:viewmodel="using:GachaRecord.ViewModel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance viewmodel:NapViewModel}"
    mc:Ignorable="d">

    <Grid>
        <ScrollViewer
            HorizontalScrollMode="Enabled"
            VerticalScrollMode="Disabled"
            HorizontalScrollBarVisibility="Auto">
            <ui:HorizontalEqualPanel
                Margin="16"
                MinItemWidth="360"
                Spacing="16">
                <local:GachaStatistic DataContext="{Binding CharacterWish, Mode=OneWay}"/>
                <local:GachaStatistic DataContext="{Binding WeaponWish, Mode=OneWay}"/>
                <local:GachaStatistic DataContext="{Binding StandardWish, Mode=OneWay}"/>
                <local:GachaStatistic DataContext="{Binding BangbooWish, Mode=OneWay}"/>
            </ui:HorizontalEqualPanel>
        </ScrollViewer>
    </Grid>
</Page>
